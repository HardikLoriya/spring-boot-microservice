server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/HardikLoriya/spring-boot-microservice/blob/develop/config-server/application.yml
          clone-on-start: true