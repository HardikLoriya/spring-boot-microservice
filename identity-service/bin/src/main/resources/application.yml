spring:
  application:
    name: IDENTITY-SERVICE
  datasource:
    url: jdbc:h2:mem:IdentityService;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect    

eureka:
  client:
    service-url:
      defaultZone : http://localhost:8761/eureka/


server:
  port: 9898